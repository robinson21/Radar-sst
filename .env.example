PORT=3000
SCAN_INTERVAL_MINUTES=1440
REMOTE_FETCH_ENABLED=true

# Optional for production/web deployment.
# If set, app state and snapshots use Neon Postgres instead of local files.
DATABASE_URL=postgresql://user:password@host/dbname?sslmode=require
